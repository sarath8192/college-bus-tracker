apiVersion: v1
kind: Service
metadata:
  name: college-bus-frontend-service
  namespace: college-bus
spec:
  type: NodePort
  selector:
    app: college-bus-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080